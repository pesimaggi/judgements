{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "cronSchedule": "0 */2 * * *",
    "startCommand": "npm run db:deploy && INGEST_MAX_PAGES=200 npm run ingest -- --adapter=icelandic-courts",
    "restartPolicyType": "NEVER"
  }
}
