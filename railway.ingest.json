{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "cronSchedule": "0 6 * * 1",
    "startCommand": "npm run db:deploy && INGEST_MODE=recent INGEST_MAX_PAGES=40 npm run ingest -- --adapter=icelandic-courts && npm run ingest -- --adapter=lagasafn && npm run ingest -- --adapter=efta-court && npm run ingest -- --adapter=citations",
    "restartPolicyType": "NEVER"
  }
}
